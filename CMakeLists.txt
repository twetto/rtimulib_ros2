cmake_minimum_required(VERSION 3.8)
project(rtimulib_ros2)

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(std_msgs REQUIRED)
find_package(rosidl_default_generators REQUIRED)

rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/CameraTrigger.msg"
  DEPENDENCIES std_msgs
)

#  This package needs a PATCHED RTIMULib, carried as a submodule at
#  third_party/RTIMULib. Stock upstream builds and runs, but silently degrades:
#  without the block-read patch the per-sample I2C cost exceeds the 119 Hz
#  budget and the node publishes about 92 Hz instead of 117.5, and without the
#  correction switches the raw mode this package depends on does not exist, so
#  the gyro arrives with its bias already removed and any noise characterisation
#  measured from it is wrong. Neither failure announces itself.
set(RTIMULIB_SOURCE_DIR
  "$ENV{RTIMULIB_SOURCE_DIR}"
  CACHE PATH "Path to the RTIMULib/RTIMULib source directory")

if(NOT RTIMULIB_SOURCE_DIR)
  #  Submodule only. There is deliberately no fallback to a sibling checkout:
  #  an unpinned tree next to this one is indistinguishable from the pinned one
  #  at build time, so a stale or unpatched copy would be picked up silently -
  #  exactly the failure this submodule exists to prevent.
  get_filename_component(RTIMULIB_SOURCE_DIR
    "${CMAKE_CURRENT_SOURCE_DIR}/third_party/RTIMULib/RTIMULib" ABSOLUTE)
endif()

if(NOT EXISTS "${RTIMULIB_SOURCE_DIR}/RTIMULib.h")
  message(FATAL_ERROR
    "RTIMULib not found at ${RTIMULIB_SOURCE_DIR}\n"
    "  This package carries a patched RTIMULib as a submodule. Fetch it with:\n"
    "      git submodule update --init --recursive\n"
    "  Stock upstream RTIMULib will build but degrades silently: about 92 Hz\n"
    "  instead of 117.5, and gyro_bias_correction has no effect.\n"
    "  To use a tree elsewhere, set RTIMULIB_SOURCE_DIR explicitly.")
endif()

message(STATUS "RTIMULib source: ${RTIMULIB_SOURCE_DIR}")

add_subdirectory("${RTIMULIB_SOURCE_DIR}" "${CMAKE_CURRENT_BINARY_DIR}/rtimulib")

add_executable(rtimulib_node src/rtimulib_node.cpp)
target_include_directories(rtimulib_node PRIVATE "${RTIMULIB_SOURCE_DIR}")
target_link_libraries(rtimulib_node RTIMULib)
ament_target_dependencies(rtimulib_node rclcpp sensor_msgs std_msgs)

#  The trigger message is generated by this same package, so link the generated
#  C++ typesupport directly rather than depending on an external interface pkg.
rosidl_get_typesupport_target(cpp_typesupport_target ${PROJECT_NAME} "rosidl_typesupport_cpp")
target_link_libraries(rtimulib_node "${cpp_typesupport_target}")

install(TARGETS rtimulib_node
  DESTINATION lib/${PROJECT_NAME})

install(DIRECTORY config launch
  DESTINATION share/${PROJECT_NAME})

ament_package()
